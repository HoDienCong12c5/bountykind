import { TransformId } from "./transform-id";
export declare class MatrixComponent {
    private _parent;
    private _update;
    private _id?;
    private _array;
    constructor(_parent: TransformId, size: number, _update: (array: Float32Array) => void);
    get array(): Float32Array;
}
